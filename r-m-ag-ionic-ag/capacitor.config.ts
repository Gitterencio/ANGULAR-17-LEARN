import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'r&m-ag-ionic-ag',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
